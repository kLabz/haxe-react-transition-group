# Haxe externs for `react-transition-group`

See the package's [documentation](http://reactcommunity.org/react-transition-group/).

Note: this needs a recent version of `react-types` and `react-next` (instead of `react`).
